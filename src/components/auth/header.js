import React from 'react';

const Header = (props) => {
  return (
    <auth-header>
      <auth-header-logo>Listable</auth-header-logo>
      <auth-header-description>create share learn</auth-header-description>
    </auth-header>
  );
};

export default Header;
